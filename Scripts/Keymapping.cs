using UnityEngine;

public class Keymapping : MonoBehaviour
{/*
    [System.Serializable]
    public struct KeyAction
    {
        public KeyCode keyCode;
        public string actionName;
    }

    public KeyAction[] keyActions;

    private InteractionScript interactionScript;

    private void Start()
    {
        interactionScript = FindObjectOfType<InteractionScript>();
    }

    private void Update()
    {
        foreach (KeyAction keyAction in keyActions)
        {
            if (Input.GetKeyDown(keyAction.keyCode))
            {
                if (keyAction.actionName == "Interact")
                {
                    interactionScript.PerformInteraction();
                }
                else
                {
                    // Handle other actions based on keyAction.actionName
                }
            }
        }
    }*/
}
